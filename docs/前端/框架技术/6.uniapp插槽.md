# 6.uniapp 插槽

## 插槽的作用

插槽用来给组件预留内容分发位置，让组件既有统一结构，又能保留一定灵活性。

## 常见类型

- 默认插槽：只定义一个内容区域。
- 具名插槽：一个组件里有多个可插入位置。
- 作用域插槽：子组件把数据暴露给父组件渲染。

## 适用场景

- 通用弹窗、卡片、列表项
- 头部、底部、按钮区可定制的组件
- 渲染逻辑由父组件决定，但布局由子组件维护

## 注意点

- 插槽数量过多会让组件 API 变得难以理解。
- 作用域插槽灵活，但会提高使用复杂度。
- 在多端场景下，尽量让插槽结构简单明确。
